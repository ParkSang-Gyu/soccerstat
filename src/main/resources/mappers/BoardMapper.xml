<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.green.soccerstat.dao.BoardDAO">
	<select id="getList" resultType="kr.green.soccerstat.vo.BoardVO">
		select * from list where valid = 'I'
  		<if test="type==1">
  			and title like concat('%',#{search},'%')
  		</if>
  		<if test="type==2">
  			and contents like concat('%',#{search},'%')
  		</if> 
  		<if test="type==3">
  			and writer like concat('%',#{search},'%')
  		</if>  
  		order by listNo desc 
  		limit #{pageStart},#{perPageNum}
	</select>
	<select id="getCountBoardList" resultType="int">
		select count(*) from list where valid='I'
		<if test="type==1">
  			and title like concat('%',#{search},'%')
  		</if>
  		<if test="type==2">
  			and contents like concat('%',#{search},'%')
  		</if> 
  		<if test="type==3">
  			and writer like concat('%',#{search},'%')
  		</if>    
  	</select>
	<select id="getDisplay" resultType="kr.green.soccerstat.vo.BoardVO">
		select * from list 
		join reply on list.listNo = reply.listNo
		where list.listNo = #{listNum}
	</select>
</mapper>